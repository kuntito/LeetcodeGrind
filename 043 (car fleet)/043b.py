class Solution:
    def carFleet(self, target: int, position: list[int], speed: list[int]) -> int:
        # when a car drives, you want to know if it's new position exceeds
        # any of the new positions of the cars after it

        # create a set of start positions
        pass