# https://leetcode.com/problems/word-ladder/

i'm given two string variables, `beginWord` and `endWord`.
i'm also given a list of strings, `wordList`.

`beginWord` can change into another word but only one character at a time.

for example, `cat` can become `car`

however, this change can only happen if the new word exists in `wordList`