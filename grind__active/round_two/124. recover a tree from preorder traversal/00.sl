i'm given a string.
i want to return a binary tree.

the string represents the traversal of the binary tree.
it's nodes are numbers.

each node is described by it's number and the dashes preceeding it.

the dashes represent the the depth of the node.

         1
        2 3

for example,
this node would be presented 