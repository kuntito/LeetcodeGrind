# https://leetcode.com/problems/text-justification/description/

i'm given a list of words and want to make it into a paragraph, but there's a condition.

in the paragraph, every line except the last must have the same number of characters.

this number is defined as `maxWidth`

the last line may have less than max width, but it can't exceed it.

