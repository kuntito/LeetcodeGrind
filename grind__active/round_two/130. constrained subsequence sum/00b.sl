i want to return a number
after receiving two inputs.

the first input, is an array, `nums`.
it's an array of numbers.

my job is to find the subsequence within `nums`
with the largest element sum.

where the elements meet a certain condition.

what is this condition?
it affects any two consecutive numbers
where the left number is smaller than the right number.

for every pair that meets this condition
their difference cannot be less than `k`.

this is really a condition shaping, 
how i build the subsequence.

you can summarise this to..
of every valid subsequence,
which one has the largest element sum.

***
i've completely missed the plot.